public class InCorrectTest {
	// Klassen Variablen
	static int x;
	static int y;
	static int z;

	static String x_str;
	static String y_str;
	static String z_str;

	static boolean x_bool;
	static boolean y_bool;
	static boolean z_bool;

	public static void bool_Test(boolean b1, boolean b2) {
		if(1 && b2){
		z_bool = "666";
		}
		if(b1 || "b2"){
		z_bool = b2;
		}
		if (x == x_bool) {
        z_bool = ! b1;
        } else {
		z_bool = ! 26747;
		}
		if (z_str != z) {
        z_bool = true;
        }
		if (1<y_str) {
		if (x_str>4) {
		z_bool = false;
        }
        }
		if (5<="6") {
		if ("Nein">=8) {
		z_bool = z;
        }
        }
		return;
	}

	public static void String_test(){
		String z_str;
		z_str = 0;
		y_str = z_str;
		{
			String z_str;
			z_str = "Ich bin ein Nested_Block";
			x_str = z_str;
		}
		return "I like coffee";
    }

	public static int math_test(int input) {
	x = 1 + "1";
	y = true - 4;
	z = x * false;
	x = "input" / z;
	y = "input" ** x;
	z = 100 + true ** 2 - 5;
    x = 100 + 5 % 102;
    y = (100 + 5) % 102;
	input = x + y;
	return;
	}

	public static void paramTest(int param_int, String param_str, boolean param_bool){
    return;
    }

	// Main mit Parametern
	public static void main(int param_int, String param_str, boolean param_bool) {
		// lokale Variable
		boolean result;
		int i;
		String testing;
		int switch_val;

		// Zuweisung
		result = 987;
		i = "0";
		testing = "Es SOLLTEN nur Type Checker Fehler auftreten.";

		x_bool = - false;
		y_bool = true;

		// Aufrufe
		switch_val = math_test(z, "zu viel");
		String_test();
		bool_Test(x_bool);
		paramTest(x_str, x_bool, x);

		// While Schleife
		do {
            i = i + 1;
        } while (i < 1000)

		//switch mit print Statement
		switch (switch_val) {
            case x:
                System.out.println("I am finished with int 10");
            case - -y:
                System.out.println("I am finished with int 100");
			case 100 - 10:
                System.out.println("I am finished with int 90");
            case 90:
                System.out.println("I am finished with int 90");
            case 100 + z * 5 / 70 - 80 + 1 + -7000 * 7:
                System.out.println("I didn't crash");
            default :
                System.out.println("I am too much, please remove me");
            default :
                System.out.println("I am finished with the default");
        }
	}
}