public class TypeErrorTest {
  static int x;
  static boolean flag;

  public static void main() {
    int a;
    
    a = 5;
    x = a + flag;  // Error: can't add int and boolean
    flag = a;      // Error: can't assign int to boolean
  }
}
