public class SimpleIfOnlyTest {
    static int x;
    
    public static void main() {
        int a;
        
        a = 5;
        
        // Test simple if
        if (a > 0) {
            x = a * 2;
        }
        
        println(x);
    }
}